{
  "name": "cloudvault",
  "private": true,
  "packageManager": "npm@11.6.0",
  "workspaces": [
    "packages/*",
    "infrastructure"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck"
  },
  "devDependencies": {
    "turbo": "^2.7.3",
    "typescript": "^5.3.3"
  }
}